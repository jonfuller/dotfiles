# bashrc is executed by bash for non-login shells.

<% if RUBY_PLATFORM =~ /darwin/ %>
<% #####  ON MAC ###### %>
<%  %>
# bash prompt (git)
[[ -s $HOME/.bash_prompt ]] && source $HOME/.bash_prompt

# Load RVM
[[ -s "$HOME/.rvm/scripts/rvm" ]] && source "$HOME/.rvm/scripts/rvm"
<% else %>
<% #####  NOT ON MAC ###### %>
<%  %>
<% end %>

<% require 'socket' %>
<% if Socket.gethostname =~ /Colossus/ %>
<%  %>
<% #####  ON SEP NETWORK ##### %>
<% else %>
<% #####  NOT ON SEP NETWORK ##### %>
<%  %>

# synergy connection to colossus
alias syncolossus='ssh raiders.sep.com -l jcfuller -L localhost:24800:colossus.net.sep.com:24800'

# synergy client
alias synclient='synergyc -f localhost:24800'

# rdp connection to colossus
alias rdpcolossus='ssh raiders.sep.com -l jcfuller -L localhost:3390:colossus.net.sep.com:3389'

# git connection to git.sep.com
alias gitsep='ssh raiders.sep.com -l jcfuller -L localhost:9418:git.sep.com:9418'

<% end %>

# filesystem aliases
alias la='ls -al'
alias ll='ls -ll'

alias ..='cd ..'
alias ...='../..'
alias ....='../../..'
alias -- -='cd -'

alias e='explorer'

# git aliases
alias gst='git status'
alias gai='git add -i'
alias gap='git add -p'
alias gcm='git commit -m'
alias gpo='git push origin'
alias gpom='git push origin master'

